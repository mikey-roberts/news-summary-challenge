'use strict'

import Guardian from 'guardian-js';

const guardian = new Guardian(apiKey, false);
const key = config.MY_API_KEY;



getArticleTitle() {
  fetch("https://content.guardianapis.com/search?api-key=" + key).then(response => {
});
}

api.content.search('football', {
  tag: 'sports'
});


